# Test Fixtures

Place real match JSON files here for use as test fixtures. Each file should be named `{matchId}.json` (match detail) or `{matchId}.timeline.json` (timeline).

These stored matches are also used as the source-of-truth for feature extraction sanity checks — verify computed numbers against op.gg for the same games.
